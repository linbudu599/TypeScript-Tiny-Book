import { CAC } from 'cac';

/**
 * What'll be included:
 *
 * - release-it
 * - changesets
 * - git-cz
 * - changelog related
 * - simple mode
 * @param cli
 */
export default function useReleaseProject(cli: CAC) {}
